# config

Factory-wide configuration: scoring weights (`scoring-weights.yaml`), kill criteria (`kill-criteria.yaml`), category denylist, and any other tunables. Deterministic YAML/JSON only — no secrets here.
