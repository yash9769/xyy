# discovery

Scripts that surface candidate opportunities (categories, complaint patterns, underserved niches) using WebSearch and heuristics. Output: `candidates/<id>/opportunity.json` conforming to `schemas/opportunity.schema.json`, status=candidate.
