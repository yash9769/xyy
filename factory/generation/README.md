# generation

App scaffolding/codegen. Copies `templates/android/` into `apps/<id>/`, substitutes package name/app name/icon/theme, and Claude Code fills in feature-specific screens per the approved PRD/UX docs.
