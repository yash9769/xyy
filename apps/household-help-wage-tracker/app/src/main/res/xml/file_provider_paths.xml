<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Backup export files are written to the app's cache dir and shared via FileProvider,
         never to shared/external storage, so no storage permission is ever needed. -->
    <cache-path name="backups" path="backups/" />
</paths>
