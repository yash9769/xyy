#!/usr/bin/env bash
# Thin wrapper so `./scripts/appfactory <command>` works from repo root.
set -euo pipefail
REPO_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$REPO_ROOT/factory/cli/appfactory.py" "$@"
