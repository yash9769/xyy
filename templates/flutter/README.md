# flutter template (reserved)

Empty by default. The default Android stack is Kotlin + Jetpack Compose (see factory/ARCHITECTURE.md §3). This directory is only populated if a specific, evidenced opportunity requires cross-platform (iOS) reach.
