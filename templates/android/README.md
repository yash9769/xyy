# android template

Base Kotlin + Jetpack Compose template (MVVM, single module, Coroutines, DataStore/Room as needed). Populated in Phase 1/3. Every generated app starts from this template with package/app-name/icon/theme substituted.
