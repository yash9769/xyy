package com.appfactory.template

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder demonstrating the JVM unit-test setup (JUnit4, no Robolectric needed for plain
 * Kotlin logic). Delete once a real app built on this template has its own tests — this file
 * only proves the test task is wired up correctly.
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
