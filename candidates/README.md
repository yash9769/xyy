# candidates

One directory per opportunity under research, pre-approval: `candidates/<id>/opportunity.json` + `candidates/<id>/research/`. Moved to `approved/` on Gate 1 pass, or left here / moved to `rejected/` otherwise.
